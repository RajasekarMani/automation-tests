target.server: "localhost"
target.server: ${?targetServer}
default: {
  app: {
    ui.url: "http://"${target.server}
    service: {
      url: "http://"${target.server}
      username: "TEST"
      password: "TEST"
    }
  }
}

